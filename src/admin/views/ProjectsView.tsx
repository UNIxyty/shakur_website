import RecordListView from '../components/RecordListView';

/** Projects view — shares the mirrored list/editor logic with Services. */
export default function ProjectsView() {
  return <RecordListView kind="project" />;
}
